import Button from '@/components/Button';
import React from 'react';

function page(props) {
    return (
        <div>
            <Button>Button</Button>
        </div>
    );
}

export default page;